#:import tr waguilib.i18n.tr


<PassphrasesDialogContent>:
    orientation: "vertical"
    spacing: "20"
    size_hint_y: None
    height: "220"
    #id: passphrases_layout

    #MDTextField:
    #    hint_text: "Passphrase"


<ContainerStoreScreen>:

    on_pre_enter:
        # Lazy-init hack to workaround Clock iteration troubles
        container_delete_btn.size_hint_x = 0.5
        container_decrypt_btn.size_hint_x = 0.5
        root.get_detected_container()  # FIXME rename this!!!

    WAMainVerticalScrollView:

        WAVerticalPanel:

            WAToolbar:
                title: tr._("Containers")
                left_action_items: [['menu', lambda x: app.navigation_drawer.set_state("toggle")]]
                right_action_items: [["refresh", lambda x: root.get_detected_container(), tr._("Refresh")], ["home", lambda x: app.get_back_to_home_screen(), tr._("Back to home")],]

            WAElevatedBoxLayout:

                MDLabel:
                    text: tr._("View and manager encrypted containers")
                    padding_x: "10dp"

            WAVerticalScrollView:
                MDList:
                    id: container_table

            WAButtonsGridLayout:
                padding: [10, 10]

                WAOperationButton:
                    id: container_delete_btn
                    icon: "delete-alert-outline"
                    text: tr._("Delete")
                    on_press: root.open_dialog_delete_container()

                WAOperationButton:
                    id: container_decrypt_btn
                    icon: "eye-circle-outline"
                    text: tr._("Decrypt")
                    on_press: root.open_dialog_decipher_container()
