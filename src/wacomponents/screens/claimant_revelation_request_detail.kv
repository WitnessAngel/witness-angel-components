#:import tr wacomponents.i18n.tr


<ClaimantRevelationRequestDetailScreen>:
    cryptainer_label: ""

    on_pre_enter:

    WAMainVerticalScrollView:

        WAVerticalPanel:

            WAToolbar:
                title: tr._("Authorization request details")
                left_action_items: [["arrow-left", lambda x: root.go_to_previous_screen(), tr._("Back")]]
                right_action_items: [["home", lambda x: app.get_back_to_home_screen(), tr._("Back to home")],]  #FIXME DRY this back-to-home

            WAElevatedBoxLayout:
                MDLabel:
                    text: tr._("Container: %s") % root.cryptainer_label
                    padding_x: "10dp"

            MDList:
                id: revelation_requests_list
